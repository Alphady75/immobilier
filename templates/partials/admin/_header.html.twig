<!-- Navbar -->
<nav class="main-header navbar navbar-expand navbar-white navbar-light">
	<!-- Left navbar links -->
	<ul class="navbar-nav">
		<li class="nav-item">
			<a class="nav-link" data-widget="pushmenu" href="#"><i class="fas fa-bars"></i></a>
		</li>
		<li class="nav-item d-none d-sm-inline-block">
			<a href="{{ path('dashboard') }}" class="nav-link">Accueil</a>
		</li>
		<li class="nav-item d-none d-sm-inline-block">
			<a href="{{ path('admin_immobiliers_index') }}" class="nav-link">Biens Immobiliers</a>
		</li>
	</ul>

	<!-- SEARCH FORM --
	<form class="form-inline ml-3">
		<div class="input-group input-group-sm">
			<input class="form-control form-control-navbar" type="search" placeholder="Search" aria-label="Search">
			<div class="input-group-append">
				<button class="btn btn-navbar" type="submit">
					<i class="fas fa-search"></i>
				</button>
			</div>
		</div>
	</form>-->

	<!-- Right navbar links -->
	<ul class="navbar-nav ml-auto">
		<!-- Notifications Dropdown Menu -->
		<li class="nav-item dropdown">
			<a class="nav-link" data-toggle="dropdown" href="#">
				<i class="far fa-bell"></i>
				{% if messagesNonLu()|length >= 1 %}
				<span class="badge badge-warning navbar-badge">{{ messagesNonLu()|length }} </span>
				{% endif %}
			</a>
			<div class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
				<span class="dropdown-item dropdown-header">
					{{ messagesNonLu()|length >= 1 ? messagesNonLu()|length : 'Aucune' }} 
					Notification{{ messagesNonLu()|length >= 1 ? 's' : '' }} 
				</span>
				<div class="dropdown-divider"></div>
				{% if messagesNonLu()|length >= 1 %}
				<a href="{{ path('admin_contacts_index') }}" class="dropdown-item text-center">
					<i class="fas fa-envelope mr-2"></i> 
					{{ messagesNonLu()|length }} 
					{{ messagesNonLu()|length > 1 ? 'Nouveaux' : 'Nouveau' }} 
					message{{ messagesNonLu()|length > 1 ? 's' : '' }}
				</a>
				{% else %}
				<div class="dropdown-divider"></div>
				<span class="dropdown-item">
					<i class="fas fa-envelope mr-2"></i>  Aucun message
				</span>
				{% endif %}
				<div class="dropdown-divider"></div>
				<a href="{{ path('admin_contacts_index') }}" class="dropdown-item dropdown-footer">Boite de reception</a>
			</div>
		</li>
		<li class="nav-item">
			<a class="nav-link" data-widget="control-sidebar" data-slide="true" href="#"><i
				class="fas fa-th-large"></i></a>
			</li>
		</ul>
	</nav>
      <!-- /.navbar -->