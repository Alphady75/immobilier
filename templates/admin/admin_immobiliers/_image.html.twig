{% if immobilier.imageName %}

<img src="{{ vich_uploader_asset(immobilier, 'imageFile') | imagine_filter('large_size') }}" class="img-fluid mt-3 mb-3" 
	alt="{{ immobilier.name }}">

{% else %}

{{ include('admin/admin_immobiliers/_icone.html.twig') }}

{% endif %}