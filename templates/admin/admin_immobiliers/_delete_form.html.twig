<form method="post" class="d-inline" action="{{ path('admin_immobiliers_delete', {'id': immobilier.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce bien immobilier?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ immobilier.id) }}">
    <button class="btn btn-default text-danger btn-sm"><i class="fa fa-trash-alt"></i></button>
</form>
