<form method="post" class="d-inline" action="{{ path('admin_annonces_delete', {'id': annonce.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ annonce.id) }}">
    <button class="btn btn-danger btn-sm"><i class="fa fa-trash-alt"></i> Supprimer</button>
</form>
