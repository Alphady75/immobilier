{% extends 'admin.html.twig' %}

{% block title %}CategorieImmobilier{% endblock %}

{% block body %}
    <h1>CategorieImmobilier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_immobilier.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ categorie_immobilier.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ categorie_immobilier.slug }}</td>
            </tr>
            <tr>
                <th>Created</th>
                <td>{{ categorie_immobilier.created ? categorie_immobilier.created|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated</th>
                <td>{{ categorie_immobilier.updated ? categorie_immobilier.updated|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_categorie_immobilier_index') }}">back to list</a>

    <a href="{{ path('admin_categorie_immobilier_edit', {'id': categorie_immobilier.id}) }}">edit</a>

    {{ include('admin/admin_categorie_immobilier/_delete_form.html.twig') }}
{% endblock %}
