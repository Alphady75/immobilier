{% extends 'admin.html.twig' %}

{% block title %}CategorieAnnonce{% endblock %}

{% block body %}
    <h1>CategorieAnnonce</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_annonce.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ categorie_annonce.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ categorie_annonce.slug }}</td>
            </tr>
            <tr>
                <th>Created</th>
                <td>{{ categorie_annonce.created ? categorie_annonce.created|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated</th>
                <td>{{ categorie_annonce.updated ? categorie_annonce.updated|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_categorie_annonce_index') }}">back to list</a>

    <a href="{{ path('admin_categorie_annonce_edit', {'id': categorie_annonce.id}) }}">edit</a>

    {{ include('admin/admin_categorie_annonce/_delete_form.html.twig') }}
{% endblock %}
